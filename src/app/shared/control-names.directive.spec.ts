import { ControlNamesDirective } from './control-names.directive';

describe('ControlNamesDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlNamesDirective();
    expect(directive).toBeTruthy();
  });
});
